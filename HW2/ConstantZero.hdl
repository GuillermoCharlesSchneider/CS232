
/**
 * Constant zero gate: given input a, output is always 0
 */

CHIP ConstantZero {
    IN a;
    OUT out;

    PARTS:
    // Put your code here:
	ConstantOne(a=a, out=One);
	Not(in=One, out=out);
}
